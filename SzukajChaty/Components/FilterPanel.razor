@* Components/FilterPanel.razor *@

<div class="filter-panel">
    <input @bind-Value="City" placeholder="Miasto" />

    <select @bind-Value="Type">
        <option value="">Wszystkie typy</option>
        <option value="flat">Mieszkanie</option>
        <option value="house">Dom</option>
    </select>

    <input type="number" @bind-Value="PriceMin" placeholder="Cena od" />
    <input type="number" @bind-Value="PriceMax" placeholder="Cena do" />

    <button @onclick="OnSearchClicked">Szukaj</button>
</div>

@code {
    [Parameter] public string City { get; set; } = string.Empty;
    [Parameter] public string Type { get; set; } = string.Empty;
    [Parameter] public decimal? PriceMin { get; set; }
    [Parameter] public decimal? PriceMax { get; set; }
    [Parameter] public EventCallback OnSearch { get; set; }

    private async Task OnSearchClicked()
    {
        await OnSearch.InvokeAsync();
    }
}